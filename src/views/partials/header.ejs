<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet">
    <script src="/shared/utils.js" defer></script>
    <script type="module" src="/main.js" defer></script>
    <title>
        <%= locals.title %> | File Uploader
    </title>
</head>

<body>
    <header>
        <nav class="main-nav">
            <a href="/" class="logo-link"><img src="/icons/upload.svg" alt="upload icon" class="icon"> File Uploader</a>
            <% if (currentUser) { %>
                <a href="/logout" class="btn blue">Logout <img src="/icons/log-out.svg" alt="logout icon"
                        class="icon"></a>
                <% } else { %>
                    <a href="/login" class="btn blue">Login <img src="/icons/log-in.svg" alt="login icon"
                            class="icon"></a>
                    <% } %>
        </nav>
    </header>